<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.hyg.paintboard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PaintBoardSurfaceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
        <activity android:name=".PaintBoardActivity" />
        <activity android:name=".BestPaintBoardActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".PaintBoardSurfaceActivity"></activity>
        -->
        <activity
            android:label="@string/lbl_color_dialog"
            android:name=".ColorPaletteDialog"
            android:theme="@style/DialogTheme.NoTitle" />
        <activity
            android:label="@string/lbl_pen_dialog"
            android:name=".PenDialog"
            android:theme="@style/DialogTheme.NoTitle" />

    </application>

</manifest>